[package]
name = "update_version"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = {version = "1.0.228", features = ["derive"]}
serde_json = {version = "1.0.145"}
toml = "0.9.8"
serde_toml = "0.0.1"